.ltl files contain one or more LTL properties. Each property must be written
on a separate line predecessed by #property. Atomic propositions are
defined using standard #define macros.

Use combine script to produce LTL properties with a dve file. (See combine -h.)
